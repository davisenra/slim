services:
  php:
    build: "."
    ports:
      - "443:443/udp"
      - "443:443"
      - "80:80"
    volumes:
      - "$PWD/:/app"
      - "caddy_data:/data"
      - "caddy_config:/config"

volumes:
  caddy_data:
  caddy_config:
